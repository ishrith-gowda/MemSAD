# legacy config: isort settings for tools that read setup.cfg
# primary config lives in pyproject.toml; flake8 config in .flake8

[isort]
profile = black
line_length = 88
known_first_party = attacks,defenses,evaluation,memory_systems,watermark,utils,data,scripts
skip_glob = external/*
